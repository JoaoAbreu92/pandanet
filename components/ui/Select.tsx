import React, { SelectHTMLAttributes, forwardRef, useId } from 'react';
interface SelectProps extends SelectHTMLAttributes<HTMLSelectElement> { label?: string; error?: string; hint?: string; wrapperClassName?: string; }
export const Select = forwardRef<HTMLSelectElement, SelectProps>(({ label, error, hint, wrapperClassName = '', className = '', id, required, children, ...props }, ref) => { const generatedId = useId(); const selectId = id || props.name || generatedId; const descriptionId = error || hint ? `${selectId}-description` : undefined; return <div className={`w-full ${wrapperClassName}`}>{label && <label htmlFor={selectId} className="mb-1.5 block text-sm font-semibold text-slate-700 dark:text-slate-200">{label}{required && <span className="ml-1 text-rose-500">*</span>}</label>}<select ref={ref} id={selectId} required={required} aria-invalid={!!error} aria-describedby={descriptionId} className={`min-h-10 w-full rounded-xl border bg-white px-3 py-2 text-sm text-slate-900 shadow-sm outline-none focus:border-emerald-500 focus:ring-4 focus:ring-emerald-500/15 disabled:cursor-not-allowed disabled:bg-slate-100 disabled:opacity-60 dark:bg-slate-950/40 dark:text-white ${error ? 'border-rose-500' : 'border-slate-200 dark:border-white/10'} ${className}`} {...props}>{children}</select>{(error || hint) && <p id={descriptionId} className={`mt-1.5 text-xs ${error ? 'font-medium text-rose-600 dark:text-rose-400' : 'text-slate-500 dark:text-slate-400'}`}>{error || hint}</p>}</div>; });
Select.displayName = 'Select';
